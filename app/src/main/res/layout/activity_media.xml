<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/media_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <org.fossify.commons.views.MySearchMenu
        android:id="@+id/media_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/loading_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="gone"
        app:hideAnimationBehavior="inward"
        app:showAnimationBehavior="outward"
        app:showDelay="500"
        tools:visibility="visible" />

    <RelativeLayout
        android:id="@+id/media_holder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <org.fossify.commons.views.MyTextView
            android:id="@+id/media_empty_text_placeholder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:alpha="0.8"
            android:gravity="center_horizontal"
            android:paddingStart="@dimen/activity_margin"
            android:paddingTop="@dimen/activity_margin"
            android:paddingEnd="@dimen/activity_margin"
            android:text="@string/no_media_with_filters"
            android:textSize="@dimen/bigger_text_size"
            android:textStyle="italic"
            android:visibility="gone" />

        <org.fossify.commons.views.MyTextView
            android:id="@+id/media_empty_text_placeholder_2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/media_empty_text_placeholder"
            android:layout_centerHorizontal="true"
            android:background="@drawable/ripple_all_corners"
            android:padding="@dimen/activity_margin"
            android:text="@string/change_filters_underlined"
            android:textSize="@dimen/bigger_text_size"
            android:visibility="gone" />

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/media_refresh_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.qtalk.recyclerviewfastscroller.RecyclerViewFastScroller
                android:id="@+id/media_fastscroller"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:supportSwipeToRefresh="true">

                <org.fossify.commons.views.MyRecyclerView
                    android:id="@+id/media_grid"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layoutAnimation="@anim/layout_animation"
                    android:scrollbars="none"
                    app:layoutManager="org.fossify.commons.views.MyGridLayoutManager"
                    app:spanCount="@integer/media_columns_vertical_scroll" />

            </com.qtalk.recyclerviewfastscroller.RecyclerViewFastScroller>
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
    </RelativeLayout>

    <!-- Elderly-friendly "Photos" grid (docs/elderly-spec/photos-grid.md). Full-screen overlay
         shown instead of the chrome above for a direct, non-third-party-intent launch; the
         picker-intent flow above (search menu, folder view, CAB) is left untouched. -->
    <RelativeLayout
        android:id="@+id/photos_elderly_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        tools:visibility="visible">

        <LinearLayout
            android:id="@+id/photos_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="@dimen/activity_margin"
            android:paddingVertical="@dimen/elderly_header_vertical_padding">

            <org.fossify.commons.views.MyTextView
                android:id="@+id/photos_header_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/elderly_photos_title"
                android:textSize="@dimen/elderly_header_text_size" />

            <org.fossify.commons.views.MyTextView
                android:id="@+id/photos_header_month"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/elderly_header_text_size"
                tools:text="May" />
        </LinearLayout>

        <RelativeLayout
            android:id="@+id/photos_bottom_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:padding="@dimen/elderly_bottom_bar_padding">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/photos_select_button"
                style="@style/ElderlyButton.Primary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/elderly_select_photos"
                app:icon="@drawable/ic_select_all_vector" />

            <LinearLayout
                android:id="@+id/photos_selection_bar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                tools:visibility="visible">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/photos_delete_button"
                        style="@style/ElderlyButton.Secondary"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="@dimen/small_margin"
                        android:layout_weight="1"
                        android:text="@string/delete"
                        app:icon="@drawable/ic_delete_vector" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/photos_share_button"
                        style="@style/ElderlyButton.Primary"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="@dimen/small_margin"
                        android:layout_weight="1"
                        android:text="@string/share"
                        app:icon="@drawable/ic_share_ios_vector" />
                </LinearLayout>

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/photos_cancel_button"
                    style="@style/ElderlyButton.Secondary"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/small_margin"
                    android:text="@string/cancel"
                    app:icon="@drawable/ic_cross_vector" />
            </LinearLayout>
        </RelativeLayout>

        <org.fossify.commons.views.MyTextView
            android:id="@+id/photos_empty_placeholder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/photos_header"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="@dimen/activity_margin"
            android:gravity="center_horizontal"
            android:text="@string/elderly_no_photos_yet"
            android:textSize="@dimen/elderly_body_text_size"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/photos_elderly_grid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@id/photos_bottom_bar"
            android:layout_below="@id/photos_header"
            android:clipToPadding="false"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="2"
            tools:listitem="@layout/item_photos_grid" />
    </RelativeLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
